version: 0.2

phases:
  install:
    commands:
      - npm install -g aws-cdk
      - pip install -r requirements.txt

  build:
    commands:
      - cdk synth ApiInfraStack > ApiInfraStack.template.json

artifacts:
  files:
    - ApiInfraStack.template.json
